general:
  region:
  resource_prefix:
  tags:

platform:
  management:
    user_groups:
      engineers:
        display_name: "engineers"
      analysts:
        display_name: "analysts"
      admins:
        display_name: "admins"
    resource_groups:
      infra:
        display_name: "infra"
        # iam:
        #   role_assignments:
        #     - role_name: "Owner"
        #       user_group_key: "engineers"
      data:
        display_name: "data"
    metadata_store:
    secrets_store:
  storage:
  network:
  compute:
    databricks_workspaces:
  orchestration:
    data_factories:
